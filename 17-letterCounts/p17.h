// Returns the number of character used to spell out a nubmer
int numCharCount(int num);

// Returns the number of characters needed to print all numbers
// from min to max, inclusive
int getRangedCharCount(int min, int max);
