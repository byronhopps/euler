// Returns the largest palindrome that is the product
// of two numbers with numDigits digits
int largestPalindrome(int numDigits);

// Returns 1 if num is a palindrome, 0 otherwise
int isPalindrome(int num);

// Returns 10^num
int power10(int num);
